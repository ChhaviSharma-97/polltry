pipeline {
    agent any
    stages {
        
        stage('Start') {
            steps {
                echo 'Running build automation'
                sh 'echo "Start Successful"'
                
            }
        }
    }
}
